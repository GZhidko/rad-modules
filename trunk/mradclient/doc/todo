+ использовать
  IP-Source
  UDP-Port
+ scanf -> strtol
+ в командной строке задавать какие атрибуты используем
- в командной строке задавать затирать ли использованные атрибуты

- использовать Radclient-Query-Id для отчиски пула при таймаутах
  и вообще, если нет RAD-Identifier
  (это потребует существенной доработаки; и подумать надо)

- статистика тайаутов
- протоколирование с уровнями

- следует сделать отдельный класс
  для хранения данных (что-нибудь
  простое: {char * data, int len})
  и использовать этот класс во всех
  буферах.
  это надо сделать обязательно перед
  реализацией очередей в Heaver
  и использовать в очередях уже
  этот класс.
   мысли:
   - видимо хранить надо
     - данные
     - длину
     - время создания
   - надо предусмотреть хорошую сериализацию
- то-ли при выходе из диспетчера, то-ли при очистке диспетчера,
  то-ли вообще никогда; но наверно надо бы вываливать
  в лог необработанное содержимое буферов;
  возможно это надо делать прям в деструкторе буфера,
  но это решение чревато двумя проблемами:
  - не будет ли зацикливаний
  - каждый буфер должен иметь ссылку на logger-объект
- система сборки:
  - объектники в отдельно директории
  - исполняемый файл в корне
  - make dist должен паковать doc/ и tools/
- надо бы в протоколировании убрать С-шние форматы
  и сделать всё более С++-но
